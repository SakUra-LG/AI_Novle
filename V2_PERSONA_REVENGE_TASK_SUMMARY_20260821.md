# V2 重生复仇小说生成阶段总结（人物塑造与复仇爽点）

更新时间：2026-08-21  
项目：V2《重生天王》自动生成系统  
本轮状态：已停止继续推进，等待下一次明确启动

## 一、任务目标

本轮目标是在已有 V2 生成流程上，优先形成可用产出，同时补强两项导师要求：

1. 人物不能只用“好人/坏人”二分，而要通过性格根源、价值冲突、选择代价和关系变化塑造人物。
2. 重生小说的核心爽点必须来自“前世受害信息差”：主角知道上一世的陷阱、责任链和结果，在今生提前布局，让对手因轻视而入局，最后发生可见的损失、权力倒置和因果清算。

本轮明确采用“产出优先”策略：非结构性、非连续性致命问题不反复阻塞生成；但时间线、角色生死、章节归属和 Neo4j 图谱连续性仍然保留硬校验。

## 二、已完成的工作

### 1. 已阅读并落实人物塑造方法

人物塑造参考文件提出的核心原则已经落实到生成约束中：

- 同一性格根源同时产生优点和缺点，而不是强行拼接“外冷内热”。
- 人物形象必须由动作、犹豫、撒谎、取舍、牺牲和关系变化体现。
- 重要场景至少让一名核心人物在两个都有价值或都有代价的选项之间作出选择。
- 禁止用“其实他很善良”“她意识到自己改变了”等旁白直接解释人物。
- 反派可以滑稽，但滑稽必须造成误判、暴露证据或现实损失，不能只是插科打诨。

### 2. 建立人物约束文件并接入正文生成

人物约束文件：

`bert_excitation_train/data/pop_king_character_bible_v1.json`

当前约束包含：

- `trait_root`：性格根源
- `surface_mask`：外在面具
- `hidden_fear`：隐藏恐惧
- `strength_from_root` / `shadow_from_root`：同一性格根源形成的能力与阴影
- `self_justification`：人物如何为自己辩护
- `moral_boundary`：道德边界
- `choice_pressure`：触发选择的压力条件
- `speech_rules` / `action_signature`：对白和动作习惯
- `relationship_arc`：关系变化方向

正文脚本会按当前场景人物提取对应约束；人物固定性格来自该文件，当前伤病、秘密、关系和已支付代价仍以章节卡与 Neo4j 检索结果为准，避免人物设定覆盖剧情事实。

### 3. 完成全书规划产出

全书规划已形成并保存在：

- `outputs_pop_king_v6_compiled_story_first_500/event_clusters_v2.json`：250 个情节事件族
- `outputs_pop_king_v6_compiled_story_first_500/chapter_synopses_v5_qwen_500.json`：500 章章节梗概
- `outputs_pop_king_v6_compiled_story_first_500/master_ctx_cards_v2.json`：500 章上下文卡

数量已核对：250 个事件族、500 张章节卡。

规划中的每个事件族已经包含重生相关字段，例如前世信息差、今生反击动作、对手反应、阶段收益与下一步推进。规划生成脚本为：

- `bert_excitation_train/scripts/v2/generate_pop_king_500_plan.py`
- `bert_excitation_train/scripts/v2/pop_king_plan_compiler.py`
- `bert_excitation_train/scripts/v2/generate_pop_king_500_qwen.py`

### 4. 接入 Neo4j 图数据库连续性机制

当前规划故事 ID：`planning-be26ba6bb2c83203`

Neo4j 用于保存并检索：

- 章节时间线和事件顺序
- 角色身份、生死和当前状态
- 已发生的秘密、证据、关系变化和代价
- 章节正文提交后的 StoryMemory

正文生成脚本在写下一章前读取图谱上下文，并在接受正文后写回章节记忆。已经核对当前正文对应的章节记忆数量为 40 份。

### 5. 更新正文生成脚本

正文脚本：

`bert_excitation_train/scripts/v2/generate_pop_king_body_v5.py`

已完成的关键更新：

- 支持 OpenAI 兼容的 Qwen 中转站接口；本轮实际使用 `qwen-plus`。
- 将人物约束注入场景提示词。
- 强制重生信息差转化为今生的具体抢先动作。
- 检查复仇目标、主动布局、对手误判、现实损失和权力倒置。
- 保留 Neo4j 的时间线、角色状态和生死连续性硬校验。
- 增加生产优先模式：对部分非致命语义提醒记录警告而不阻塞正文产出。
- 失败正文通过模型重新生成，不采用人工直接拼接正文的方式伪造通过。

### 6. 已生成正文产出

当前已经接受并保存：

- 第 1–40 章正文，共 40 个章节文件
- EC001–EC020，共 20 个两章事件族正文批次
- 每个批次都有 provenance 记录和原始模型响应哈希
- 正文对应的 Neo4j chapter memory 共 40 份

正文目录：

`outputs_pop_king_v6_compiled_story_first_500/chapters/`

生成记录目录：

`outputs_pop_king_v6_compiled_story_first_500/body_generation/provenance/`

本轮没有生成第 41 章之后的新正文；后台残留的规划进程也已经停止。

## 三、当前可以作为后续正文依据的文件

后续正文生成时应同时读取以下几类文件，而不能只依赖单一 Markdown：

1. 全书事件族：`event_clusters_v2.json`
2. 全书章节卡：`chapter_synopses_v5_qwen_500.json` / `master_ctx_cards_v2.json`
3. 人物约束：`data/pop_king_character_bible_v1.json`
4. 已生成正文：`chapters/chapter_*.txt`
5. 正文生成 provenance：`body_generation/provenance/EC*.json`
6. Neo4j 故事图谱及章节记忆：故事 ID `planning-be26ba6bb2c83203`
7. 人工审查队列：`manual_review_queue_v1.md`

其中，章节卡和事件族决定“要发生什么”，人物约束决定“人物在矛盾中如何行动”，正文与 Neo4j 决定“之前已经发生了什么以及当前状态是什么”。

## 四、目前存在的问题

### 1. 正文只完成到第 40 章

全书规划已经是 500 章，但正文不是 500 章。目前只有 1–40 章，不能把规划产出误称为全书正文完成。

### 2. 人工人物审查尚未完成

人物约束已经接入模型，但这不等于每个事件族和每章都经过人工审读。人工审查队列中仍有以下类型问题：

- 用“眼神坚定”“表面平静”等姿态替代真正选择。
- 用“命运共同体”“绝对信赖”等抽象关系词替代权限、秘密和风险承担的具体变化。
- 用“标志着”概括权力更替，却没有写出签字权、资金、职位或证据控制权的实际变化。
- 个别事件的角色身份称谓需要和角色表统一。

详细清单见 `outputs_pop_king_v6_compiled_story_first_500/manual_review_queue_v1.md`。

### 3. 生产优先模式保留了非致命警告

部分章节仍可能出现语义重合、结算动作不够明显或单章复仇推进偏弱等警告。它们没有被当作致命错误阻塞生成，但需要后续人工抽查，尤其要检查“主角是否主动布局”和“反派是否意识到反击来自主角”。

### 4. 复仇主脊柱仍需继续强化

当前规划已经具备重生信息差字段，但后续正文仍应持续让读者看见完整链条：

`受辱/受害 → 明确记账对象 → 主角蓄力 → 对手轻视 → 反转 → 现实损失 → 被主角或公众见证的清算`

如果只写城市犯罪、调查和悲惨事件，而没有明确“谁欠了主角什么、主角如何亲手讨回”，作品会重新滑向群像犯罪史，而不是重生复仇爽文。

### 5. “复仇账本”还没有完全结构化

已有事件和章节字段可以表达债务，但尚未把所有债务统一为独立、可查询、可结算的 `active_debts` 结构。后续若不补齐，长篇生成时容易出现欠债对象遗忘、重复清算或结算方式与前文不一致。

## 五、后续改进建议（按优先级）

### P0：继续产出正文

从第 41 章开始按两章一个事件族分批生成，保持当前生产优先策略。每批生成后必须：

1. 读取前序正文和 Neo4j 状态。
2. 检查角色生死、身份、证据和关系连续性。
3. 检查本批是否出现重生信息差带来的抢先动作。
4. 接受后写入正文文件、provenance 和 Neo4j chapter memory。

### P1：补建复仇账本

为每笔债务建立稳定 ID，至少包含：

`debt_id`、欠债人、受害对象、具体行为、前世受害章节、今生取证动作、当前状态、计划清算章节、清算方式、现实损失、见证者。

生成器每次调用读取未结算债务；正文接受后更新状态为 `active`、`partially_settled` 或 `settled`，从而保证爽点不会遗忘或重复使用。

### P1：增强人物选择审查器

在现有语义审查之外增加结构化检查：

- 本章是否存在两个有价值/有代价的选项。
- 人物是否做出可观察的选择。
- 选择是否带来明确代价。
- 选择是否改变至少一段关系、权限或资源。
- 是否出现直接解释人物内心的禁用句式。

### P1：增强复仇爽点审查器

建议记录以下分数或布尔字段：

- `grievance_clarity`：受害和委屈是否具体
- `target_clarity`：责任对象是否明确
- `agency`：主角是否主动布局
- `setup_payoff`：是否兑现此前埋点
- `power_reversal`：是否发生身份/权力倒置
- `poetic_justice`：反派依赖的东西是否反噬反派
- `witness_effect`：反转是否被对手或旁观者看见
- `settlement_cost`：清算是否付出代价

### P2：全量人工审查并重新同步图谱

完成 250 个事件族和 500 张章节卡的人工抽查后，修改正式 JSON，重新运行全量校验，再同步 Neo4j。正式正文冻结前，应保留修订前后哈希和审查记录。

## 六、结束时的实际状态

- 本轮目标已结束，未标记为“全书正文完成”。
- 当前正文产出：1–40 章。
- 当前规划产出：250 个事件族、500 章章节梗概/章节卡。
- 当前图数据库章节记忆：40 份；规划故事 ID 为 `planning-be26ba6bb2c83203`。
- 当前正文模型：`qwen-plus`（OpenAI 兼容中转站）。
- 当前没有继续运行的正文或规划生成进程。
- 后续开始前，应先确认是否继续从第 41 章生成，而不是重新从第 1 章开始。
